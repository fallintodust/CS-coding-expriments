# 排序算法实验报告 - 截图指导手册

## 截图要求总结

每个测试结果都需要满足"代码、输入文件内容、运行结果界面在同一张图中"的要求。

### 2.2.1 归并排序算法截图
**需要显示的内容：**
1. **代码窗口**：VSCode中打开 `sorting/merge_sort.c` 文件
2. **输入文件**：显示 `tests/random_data_n10000.txt` 的前10-15行内容
3. **运行结果**：终端中运行归并排序相关测试的输出

**建议截图布局：**
- 左侧：VSCode文件树 + merge_sort.c代码
- 右上：random_data_n10000.txt文件内容（head -15命令）
- 右下：./tests/demo_three_sorts 运行结果（重点显示归并排序部分）

**关键命令：**
```bash
head -15 tests/random_data_n10000.txt
./tests/demo_three_sorts
```

### 2.2.2 快速排序算法截图
**需要显示的内容：**
1. **代码窗口**：VSCode中打开 `sorting/sort.c`，显示快速排序相关函数
2. **输入文件**：显示随机数据文件内容
3. **运行结果**：快速排序的测试输出

**建议截图布局：**
- 左侧：VSCode显示sort.c中的quick_sort和partition函数
- 右侧：终端显示输入数据和运行结果

### 2.3.1 数量级10000性能比较截图
**需要显示的内容：**
1. **代码窗口**：VSCode中打开 `tests/bench_runner.c`
2. **输入文件**：显示 `tests/random_data_n10000.txt` 内容（前10行）
3. **运行结果**：显示所有排序算法的性能对比结果

**关键命令：**
```bash
head -10 tests/random_data_n10000.txt
./tests/bench_runner 10000
```

**预期输出：**
```
Random data saved to tests/random_data_n10000.txt
bubble avg=222.856181 ms
selection avg=132.805609 ms
insertion avg=70.480087 ms
merge avg=0.952914 ms
quick avg=0.571210 ms
heap avg=1.891842 ms
```

### 2.3.2 数量级100000性能比较截图
**需要显示的内容：**
1. **代码窗口**：同上，bench_runner.c代码
2. **输入文件**：显示 `tests/random_data_n100000.txt` 内容（前10行）
3. **运行结果**：显示100000规模的性能测试结果

**关键命令：**
```bash
head -10 tests/random_data_n100000.txt
./tests/bench_runner 100000
```

**预期输出：**
```
Random data saved to tests/random_data_n100000.txt
bubble avg=25804.839185 ms
selection avg=15468.407314 ms
insertion avg=8125.992056 ms
merge avg=13.907481 ms
quick avg=8.814900 ms
heap avg=33.942955 ms
```

### 2.3.3 数量级1000000性能比较截图
**需要显示的内容：**
1. **代码窗口**：VSCode中打开 `tests/bench_nlogn_only.c`
2. **输入文件**：显示生成的百万级数据文件前几行
3. **运行结果**：仅O(nlogn)算法的性能测试结果

**关键命令：**
```bash
head -10 tests/random_data_n1000000.txt  # 如果文件存在
./tests/bench_nlogn_only 1000000
```

**预期输出：**
```
=== O(nlogn) 排序算法性能测试 (N=1000000) ===
算法            平均时间(ms)
--------------------------------
merge           195.898
quick           105.897
heap            409.460

验证排序正确性: 正确
```

## 截图技巧

1. **VSCode布局建议**：
   - 使用Split Editor功能同时显示代码和数据文件
   - 打开集成终端在底部显示命令输出
   - 调整窗口大小使所有内容清晰可见

2. **终端命令顺序**：
   ```bash
   # 显示输入文件
   head -10 tests/random_data_n10000.txt
   
   # 运行测试
   ./tests/bench_runner 10000
   
   # 查看结果文件（可选）
   cat tests/results_n10000.txt
   ```

3. **最佳截图时机**：
   - 在运行测试命令后，确保输出完整显示
   - 确保代码窗口显示关键算法实现
   - 确保输入数据文件内容可见

## 文件位置快速参考

- 归并排序代码：`sorting/merge_sort.c`
- 快速排序代码：`sorting/sort.c`（quick_sort函数）
- 基准测试程序：`tests/bench_runner.c`
- O(nlogn)专用测试：`tests/bench_nlogn_only.c`
- 随机数据文件：`tests/random_data_n*.txt`
- 结果文件：`tests/results_n*.txt`